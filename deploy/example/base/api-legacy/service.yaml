apiVersion: v1
kind: Service
metadata:
  name: api-legacy
  labels:
    app.kubernetes.io/component: api-legacy
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/component: api-legacy