# Resort Management System - Environment Variables Example
# Copy this file to .env only if you need to override default values
# Default values are optimized for local development

# MySQL Configuration (defaults for local development)
MYSQL_ROOT_PASSWORD=root

# API Core MySQL Credentials
MYSQL_CORE_USER=rms_core
MYSQL_CORE_PASSWORD=rms_core_pass

# API Legacy MySQL Credentials
MYSQL_LEGACY_USER=rms_legacy
MYSQL_LEGACY_PASSWORD=rms_legacy_pass

# Database Names
MYSQL_DATABASE_LEGACY=rms-legacy
MYSQL_DATABASE_CORE=rms-core

# Service Ports
API_CORE_PORT=8080
API_LEGACY_PORT=8081
FRONTEND_PORT=9000
MYSQL_PORT=3306
REDIS_PORT=6379

# JWT Configuration
JWT_SECRET=local-development-secret
JWT_ACCESS_TOKEN_EXPIRY=900        # 15 minutes
JWT_REFRESH_TOKEN_EXPIRY=604800    # 7 days

# Spring Profiles
SPRING_PROFILES_ACTIVE=local

# Go Profile
PROFILE=local

# Node Environment
NODE_ENV=development

# Docker Build Target (development or production)
BUILD_TARGET=development

# Frontend API URL (for production builds)
API_URL=http://api-core:8080

# Redis External Connection (when not using Docker Redis)
# REDIS_HOST=your-redis-host
# REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password
